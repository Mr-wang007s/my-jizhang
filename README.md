# 个人记账小程序

一个使用 Taro + React + TypeScript 开发的个人记账应用，采用 iOS 设计风格。

## ✨ 特性

- 📱 **多端支持** - 基于 Taro 4.x，支持微信小程序、支付宝小程序、H5 等多个平台
- 🎨 **iOS 设计风格** - 采用 iOS 设计规范，界面简洁优雅
- 💾 **本地存储** - 使用 Taro Storage API 进行数据持久化
- 📊 **数据统计** - 支持按周/月/年查看收支统计，分类统计可视化
- 🔄 **Redux 状态管理** - 使用 Redux + Redux Thunk 进行状态管理
- 📝 **TypeScript** - 完整的 TypeScript 支持，类型安全

## 🏗️ 技术栈

- **框架**: Taro 4.1.8
- **UI 库**: NutUI React Taro
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **状态管理**: Redux + Redux Thunk
- **构建工具**: Vite
- **日期处理**: Day.js
- **包管理**: pnpm

## 📦 项目结构

```
my-jizhang/
├── src/
│   ├── actions/          # Redux actions
│   │   ├── transaction.ts
│   │   └── category.ts
│   ├── reducers/         # Redux reducers
│   │   ├── transaction.ts
│   │   ├── category.ts
│   │   └── index.ts
│   ├── store/           # Redux store 配置
│   │   └── index.ts
│   ├── components/      # 可复用组件
│   │   ├── CLoading/    # 加载组件
│   │   └── TransactionItem/  # 交易项组件
│   ├── pages/           # 页面
│   │   ├── index/       # 首页 - 交易列表
│   │   ├── add/         # 添加/编辑交易
│   │   ├── statistics/  # 统计页面
│   │   └── settings/    # 设置页面
│   ├── constants/       # 常量和类型定义
│   │   ├── transaction.ts
│   │   ├── category.ts
│   │   └── commonType.ts
│   ├── services/        # 服务层
│   │   └── storage.ts   # 本地存储服务
│   ├── utils/           # 工具函数
│   │   ├── date.ts      # 日期处理
│   │   ├── calculation.ts  # 金额计算
│   │   └── common.ts    # 通用工具
│   ├── styles/          # 全局样式变量（已迁移到 Tailwind）
│   │   └── _variables.scss  # iOS 风格变量（保留用于组件）
│   ├── assets/          # 静态资源
│   ├── app.tsx          # 应用入口
│   ├── app.config.ts    # 应用配置
│   └── app.css          # 全局样式 (Tailwind + 自定义)
├── config/              # 构建配置
├── package.json
└── tsconfig.json
```

## 🚀 快速开始

### 安装依赖

```bash
pnpm install
```

### 开发

```bash
# 微信小程序
pnpm dev:weapp

# H5
pnpm dev:h5

# 支付宝小程序
pnpm dev:alipay
```

### 构建

```bash
# 微信小程序
pnpm build:weapp

# H5
pnpm build:h5

# 支付宝小程序
pnpm build:alipay
```

## 📱 功能说明

### 1. 首页 - 交易列表
- ✅ 显示总支出、总收入、结余
- ✅ 按日期分组显示交易记录
- ✅ 下拉刷新
- ✅ 点击交易项进入编辑
- ✅ 长按删除交易
- ✅ 浮动按钮快速添加

### 2. 添加/编辑交易
- ✅ 支出/收入切换
- ✅ 金额输入（支持小数）
- ✅ 分类选择（图标展示）
- ✅ 日期选择
- ✅ 备注输入
- ✅ 数据验证

### 3. 统计页面
- ✅ 周/月/年 时间段切换
- ✅ 支出/收入类型切换
- ✅ 总金额和交易笔数显示
- ✅ 分类统计（金额、笔数、百分比）
- ✅ 可视化进度条

### 4. 设置页面
- ✅ 数据统计展示
- ✅ 清空数据功能
- ✅ 版本信息
- 🚧 导出数据（开发中）
- 🚧 备份数据（开发中）

## 🎨 设计系统

项目采用现代化的 iOS 设计风格，基于 Tailwind CSS 构建统一的设计系统：

### 颜色系统
- **Primary**: #007AFF (iOS 蓝) - 主要按钮、链接
- **Success**: #34C759 (收入绿) - 收入相关
- **Danger**: #FF3B30 (支出红) - 支出相关
- **Background**: 渐变背景 (#F8F9FD → #EEF0F6)
- **Card**: 玻璃态卡片 (rgba(255, 255, 255, 0.85))

### 视觉效果
- **圆角**: 8px ~ 32px (sm/md/lg/xl/xxl)
- **阴影**: 多层次阴影系统 (sm/md/lg/glass/colored)
- **玻璃态**: 毛玻璃效果 + 背景模糊
- **渐变**: 线性渐变 (135deg)
- **字体**: SF Pro Display / PingFang SC
- **动画**: 弹簧曲线 cubic-bezier(0.34, 1.56, 0.64, 1)

## 🔧 配置说明

### Tab Bar 图标

项目默认注释了 Tab Bar 配置，需要添加图标后启用：

1. 在 `src/assets/images/` 目录下添加 6 个图标文件（40x40px）:
   - `tab-home.png` / `tab-home-active.png`
   - `tab-stats.png` / `tab-stats-active.png`
   - `tab-settings.png` / `tab-settings-active.png`

2. 在 `src/app.config.ts` 中取消 `tabBar` 配置的注释

### 默认分类

系统预置了常用分类：

**支出分类**: 餐饮、交通、购物、娱乐、医疗、住房、通讯、其他

**收入分类**: 工资、兼职、理财、红包、其他

可在设置中自定义添加分类（功能待开发）。

## 📝 开发说明

### 添加新功能

1. 在 `src/constants/` 中定义 Action Types 和数据类型
2. 在 `src/actions/` 中实现 Action Creators
3. 在 `src/reducers/` 中实现 Reducers
4. 在 `src/pages/` 或 `src/components/` 中实现 UI

### 数据持久化

使用 `src/services/storage.ts` 中的 `StorageService` 进行数据操作：

```typescript
import storageService from '@/services/storage'

// 保存交易
storageService.saveTransaction(transaction)

// 获取所有交易
const transactions = storageService.getTransactions()

// 删除交易
storageService.deleteTransaction(id)
```

## 🐛 已知问题

1. Tab Bar 需要手动添加图标文件
2. 日期选择器使用快捷选项，暂未实现完整日历选择
3. 导出和备份功能待开发
4. 分类管理功能待开发

## 📄 License

MIT License

## 👨‍💻 作者

使用 Taro + React + TypeScript 开发

参考项目: [taro-music](https://github.com/lsqy/taro-music)
